
<div class="row">
  <ol class="breadcrumb">
    <li><a id="home-from-{{node.name}}" href="#home">Home</a></li>
    <li class="active">{{node.name}}</li>
  </ol>
</div>

<div class="row">
  <div class="panel panel-default">
      <h2 class="panel-heading"><span class="glyphicon glyphicon-unchecked"/>{{node.name}}<span class="pull-right"><a href="../kasper/doc{{node.url}}" target="_blank"><small>[json]</small></a></span></h2>
      <div class="panel-body">
        <p class="panel-description text-justify">{{node.description}}</p>
        {{#if node.owner}}
        <dl class="block-content dl-horizontal">
            <dt>Owner</dt>
            <dd><a href="mailto:{{node.owner}}">{{node.owner}}</a></dd>
        </dl>
        {{/if}}
      </div>     
  </div>  
</div>
