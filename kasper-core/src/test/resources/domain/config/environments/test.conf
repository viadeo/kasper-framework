myDomain {
  application {
    partner {
      api_key = MyTestApiKey
    }
  }
  infrastructure {
    partner {
      host = test.partner.host.com
    }
  }
}

# Should not exists, but if defined, overrides infrastructure definition for this environment
# it will be overriden by app configuration or by system configuration
infrastructure {
  mysql.port = 6789
}
