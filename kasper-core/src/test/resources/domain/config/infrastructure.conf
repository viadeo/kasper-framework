myDomain {
  infrastructure {
    partner {
      host = partner.host.com
      port = 4242
    }
    webservice.host = "dev.webservice.com"
  }
}

# Should not exists, but if defined will overriden by domain environment, application config or system config
infrastructure {
  mysql.port = 1234
}
