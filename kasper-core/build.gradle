// ============================================================================
//                 KASPER - Kasper is the treasure keeper
//    www.viadeo.com - mobile.viadeo.com - api.viadeo.com - dev.viadeo.com
//            Viadeo Framework for effective CQRS/DDD architecture
// ============================================================================

description = 'KASPER CORE'

dependencies {

    // Projects
    compile project(':kasper-api')
    compile project(':kasper-common')

    // Third party dependencies
    compile('com.netflix.hystrix:hystrix-core:1.3.16') {
        exclude module: 'commons-logging'
    }
    compile 'com.netflix.hystrix:hystrix-codahale-metrics-publisher:1.3.16'

    compile libraries.REFLECTIONS
    compile libraries.SPRING_CONTEXT
    compile libraries.AXON_CORE
    compile libraries.YAMMER_METRICS
    compile libraries.JCACHE
    compile libraries.VALIDATION_API
    compile libraries.GUAVA
    compile libraries.LOG4J_OVER_SLF4J

    testCompile libraries.JUNIT
    testCompile libraries.MOCKITO
    testCompile libraries.JCACHE_EHCACHE
    testCompile libraries.HIBERNATE_VALIDATOR
}
